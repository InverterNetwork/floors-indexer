---
alwaysApply: true
---

- **Never**: write migrations, envio redeploy from scratch every deployment hence db migrations are redundant
- **Never**: use `as any` or `@ts-ignore` unless absolutely necessary
- **Always**: use end to end type safety throughout the codebase