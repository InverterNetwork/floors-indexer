// Staking event handlers for Floor Markets DeFi Platform
// NOTE: Commented out until Staking contract ABI is available

/*
import {
  Staking,
} from '../generated/src/Handlers.gen'
import {
  getOrCreateAccount,
  getOrCreateToken,
  getOrCreateUserMarketPosition,
  formatAmount,
  updateUserPortfolioSummary,
} from './helpers'

// All handlers commented out until Staking ABI is available
*/
