// Presale event handlers for Floor Markets DeFi Platform
// NOTE: Commented out until PreSale contract ABI is available

/*
import {
  PreSale,
} from '../generated/src/Handlers.gen'
import {
  getOrCreateAccount,
  getOrCreateToken,
  getOrCreateUserMarketPosition,
  formatAmount,
  updateUserPortfolioSummary,
} from './helpers'

// All handlers commented out until PreSale ABI is available
*/
